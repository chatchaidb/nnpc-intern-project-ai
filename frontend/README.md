# Frontend

Next.js internal chat UI — pick an agent, chat, and see draft replies. This is
the interns' test bench; real channels (LINE OA, email) go through n8n.

```bash
npm install
npm run dev    # http://localhost:3000
```

Set `NEXT_PUBLIC_API_URL` if the backend is not on `http://localhost:8000`.
